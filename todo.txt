Todo list.  Open items only, one line each.  The rules - what belongs here, and
that a closed item is deleted rather than annotated - are in CLAUDE.md.

  CHANGELOG.md       finished work
  MANUAL_CHECKS.md   checks that need a person at a keyboard
  KNOWN_PROBLEMS.md  standing facts that will never be done
  WIP.md             longer handoff notes

- Submit dist\z80cpmw-1.0.45-store.msix to Partner Center.  Store users are on
  1.0.35, which predates 3c64be7 and still has the release filter, and every
  build before this one has the January-2038 RTC overflow.  Do NOT submit
  z80cpmw-1.0.44-store.msix, still in dist\: it predates the RTC fix.

- MANUAL_CHECKS section 15 has never been run - no guest has set a date past
  2038 in the running application, which is what 1.0.45 exists for.

- catalogv0::indexScope() still has no caller; the plan is in Config.h.  Six
  write sites, and loadSettings() calls setDownloadDirectory() (MainWindow.cpp
  :3086) before cfgMgr.load() (:3090), so the setting is unread where it is
  needed.  Nothing re-points the folder when the index changes at runtime.

- Stored release vs mounted disks: opening Settings moves the DISKS to the
  release (SettingsDialogWx.cpp:2029-2037), the Start notice tells the user to
  move the RELEASE to the disks (MainWindow.cpp:2280-2284).  Pick one; make the
  other say so.  Only moving the release resolves a slot whose id the stored
  release retired.

- DiskCatalog.cpp:1233 takes Content-Length as a 32-bit DWORD where
  HelpWindow.cpp:1301 uses DWORD64 and says why.  Unreachable today; the
  failure is a skipped completeness check.  Make them agree.

- FEATURE_PARITY.md item 5 says "ioscpm and cpmdroid still name a tag in source"
  and its "Shared concern" bullet gives ioscpm an embedded ROM to match.  Both
  are false for ioscpm: no releaseTag since ed660d5, no bundled ROM since its
  build 66, and its own cell in that row already says so.  Read 2026-09-21.

- FEATURE_PARITY.md's repository table still says ioscpm "hosts the shared
  disk-image + help release assets".  romwbw_disks publishes them; the old
  avwohl/ioscpm v1.4.x tags must stay live for pre-migration clients, which is
  the part worth keeping.
