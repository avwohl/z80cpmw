Todo list.  Open items only - finished work is written up in CHANGELOG.md and in
the commit messages that made the changes, not kept here.  A closed item is
DELETED; it does not get a paragraph explaining that it closed.

  MANUAL_CHECKS.md   checks that need a person at a keyboard or a device
  KNOWN_PROBLEMS.md  standing facts that will never be "done"
  WIP.md             longer-form handoff notes, including what a script that
                     drives this app has to know before it tries

Cite a function name, a symbol or a greppable string - never a file:line; five
of the eight line cites this file once carried went stale within twelve hours.
Tag every item with what it needs, so a session on another machine can see at a
glance what that machine can take.


Needs a Windows machine
------------------------

- [WINDOWS] DazzlerWindow::setScale sizes the window from the card's CURRENT
video mode - updateSize() uses m_dazzler->getWidth() * m_scale, which is 32 on a
fresh card - while DazzlerWindow::create() uses a fixed 128 * scale and says why
in a comment: "Fixed size for maximum resolution (128x128 in X4 2K mode).
Smaller modes will be scaled to fill this space", and paint() StretchBlt's the
mode over the whole client rect.  So setScale shrinks a window create() sized
correctly.  It now has NO caller: MainWindow::applyDazzlerState rebuilds through
create() instead, and says there that this block collapses back to one setScale
call once the function sizes from Dazzler::MAX_WIDTH.  Fix it that way or delete
it; leaving a function whose only behaviour is wrong is the third option and the
worst one.

- [WINDOWS] closing the Dazzler window does not tell MainWindow.  DazzlerWindow
answers WM_CLOSE with show(false) under the comment "Hide instead of destroy -
let main window manage lifetime", and nothing notifies the owner, so
m_dazzlerEnabled stays true and View > Dazzler stays ticked over a window that
is not on screen.  Reopening it costs two clicks, off and on again.  The fix is
a notification from DazzlerWindow's WM_CLOSE; applyDazzlerState already reads
the window's real visibility, so it needs the message rather than a new rule.

- [WINDOWS] MainWindow::applyConfig() carries its own copy of the Dazzler
create-and-show code, and it is the copy that did not get fixed: it does not
rebuild for a scale change, it does not re-port an existing card (enableDazzler
returns early when one exists), and it still show(true)s unconditionally, so
loading a profile reopens a Dazzler window the user had closed.  Routing it
through applyDazzlerState() fixes all three at once, which is why that function
was written as a function.

- [WINDOWS] docs/CONFIGURATION.md's "Wrong kind of value" bullet is now true of
only one case out of two.  It says "the next save of any kind writes the
built-in defaults over the section that was skipped ... Simply quitting the app
is enough to lose the section".  A section skipped in a file that otherwise READ
is carried through saves now and is safe; the sentence survives only for a file
that is both wrongly typed AND unreadable, where the carry is deliberately
dropped because the file has been quarantined.  The bullet does not distinguish
them, and it does not say that a carry goes back only to the file it came from,
so a user who saves a profile out of a config with a skipped section gets a
different file from the one the bullet leads them to expect.

- [WINDOWS] MANUAL_CHECKS.md's "keys as an array" step stops at "the block on
screen is the only warning the user gets".  It is now worth two more lines,
because both are behaviours that a regression would silently take away: the
block is still there after pressing OK in Settings, and still there after a
restart.

- [WINDOWS] DiskCatalog::m_downloadDir is the one member with no lock and no
argument written down for why it does not need one.  setDownloadDirectory()
writes it once, from loadSettings() at startup, and both workers plus
getDownloadDirectory() read it unsynchronised.  It is not a live bug today - no
worker can exist at the moment it is written - but that is a property of the
call order rather than of the class, and the class is the thing that grew a
documented threading contract when the two use-after-frees were closed.  Either
write the argument down beside it or give it the same treatment as the rest.

- [WINDOWS] cpmemu has Windows work of its own - the rewritten console input
path, its test harness, and the cases its suite skips for want of the platform.
This project compiles none of that code.  See cpmemu/todo.txt, which owns it and
has the measurements.


Packaging and signing
----------------------

- [WINDOWS] [RELEASE] build-msix.ps1 -Beta: REHEARSE IT UNSIGNED, AND DO NOT
SIGN IT WHILE Version.h SAYS 1.0.22.  The rehearsal is what to run, and it runs:
build-msix.ps1 -Beta -SkipBuild -SkipSign exits 0, writes
dist\z80cpmw-1.0.22-beta-unsigned.msix and its .pdb, and leaves the published
dist\z80cpmw-1.0.22-beta.msix on hash 1549C223 before and after.  A SIGNED -Beta
run is forbidden until Version.h moves off 1.0.22, because v1.0.22-beta is
published and the script names its output from Version.h: such a run re-mints
the published artifact under its own name, and nothing in the script objects.
Assert-ExeVersion compares the exe against Version.h and says nothing about a
package being replaced, and the signing-kit guard does not fire because the kit
really is where it is expected to be.

  -WhatIf IS NOT A WAY TO FIND OUT, AND THAT IS WHY THIS ITEM IS WORDED SO HARD.
  Before dacdce5 the script had no [CmdletBinding()], so PowerShell collected
  -WhatIf into $args and the run proceeded for real.  That happened on
  2026-08-28, while checking that very premise: the run made a live Azure
  Trusted Signing call and re-minted the published 1.0.22 beta package from a
  different binary.  The GitHub release was never touched and the local copy was
  restored from it - its published digest 1549c223... matches dist\ again - but
  the signing call is spent.  -WhatIf is a hard binding error there now, and
  deliberately not SupportsShouldProcess.

  What is still owed: dist\ has no z80cpmw-1.0.22-beta.pdb and 1.0.22's symbols
  stay unrecoverable.  The -Beta branch copies bin\<Configuration>\z80cpmw.pdb
  out beside the .msix and errors out if it is not there, so a beta cannot ship
  symbol-less in silence again; the first signed -Beta run on a version that has
  not shipped is what proves that end to end, and 1.0.23 is where it happens.


The disk images are behind the core
------------------------------------

READ romwbw_emu/docs/RELEASE_ORDER_2026-08-25.md BEFORE PUBLISHING ANYTHING
HERE.  It owns the sequence and the two safety rules.  z80cpmw is step 3 of that
plan and is not a safety blocker.

- [RELEASE] the bundled images are stale, and the new
packaging/scripts/verify-disk-assets.sh now says so.  Measured 2026-08-27 on the
images unpacked from the published z80cpmw-1.0.22-beta.msix, which is how they
can be got at from a machine with no Windows build: hd1k_combo.img holds a
1079-byte r8.com where romwbw_emu/src/r8.asm now builds 1792, and a w8.com
carrying neither [hostpath] in its usage line nor the capability probe;
hd1k_games.img holds NEITHER UTILITY AT ALL, so no host file transfer works from
the games disk.  The stale R8 is the
half with teeth - an unfiltered host basename makes an ambiguous FCB and
F_DELETE then erases every matching CP/M file - so that destructive bug is live
in what ships today, not only in an old lineage.  Refreshing from
romwbw_emu/disks also brings the v1.36 rebuild: W8 no longer truncates a binary
export at the first 1Ah, both utilities accept a host path containing a space,
their byte counts are 32-bit rather than the low 16 bits, and both lost a bogus
org 0100h that had been padding w8.com with 256 leading zero bytes.  Landing it
means either cutting a new ioscpm release for the catalog to point at, or
repinning DiskCatalog.cpp's RELEASE_TAG - still v1.4.5 - at a release built from
the current disks.  Then run verify-disk-assets.sh over the staged disks;
STORE_SUBMISSION.md step 3 is where it sits in the flow.

- [RELEASE] several documents go the other way when the images are refreshed,
and not before.  docs/FILE_TRANSFER.md's "does not take one yet" paragraphs and
the step 0/1 caveats in its checklist come out; README.md's "W8 does not take a
host path yet" paragraph and the two cautions under it come out with them;
HelpWindow.cpp's "File Transfer (R8 / W8)" topic now carries the same three
blocks, each with its own deletion condition in a comment above the literal -
read those rather than deleting all three at once, because the games-disk
sentence is not gated by verify-disk-assets.sh and that gate will never supply
it; and MANUAL_CHECKS.md's struck-out W8 check is re-instated.  Do not do any of
this before verify-disk-assets.sh passes on the staged images.


Needs the owner
----------------

- [DECISION] the v1.0.22-beta GitHub release is flagged Latest rather than
prerelease, unlike 1.0.16 / 1.0.17 / 1.0.21.  If the -beta channel should stay a
prerelease: gh release edit v1.0.22-beta --prerelease.

- [DECISION] build-nsis.ps1 keeps no .pdb, and neither does build-msix.ps1's
default (Store) branch.  When both channels come off one build, as 1.0.22 did,
the kept beta .pdb matches the Store binary too; when they do not, the Store
package has no symbols kept anywhere.  A call for whoever cuts the next Store
release.


Needs a Mac
------------

- [MAC] nothing open.  The SGR 90-97 item that stood here closed in ioscpm
ed0c3d6: applySGR moved to TerminalRendition.swift and now handles 90-97 and
100-107.  How it was found stale is the part worth keeping - not by a Mac, but by
grepping the tree the item described.  Every cross-port item in this file is a
claim about somebody else's code, and nothing re-reads them.


The parity document describes ports it has not read
---------------------------------------------------

- [DECISION] FEATURE_PARITY.md's row 2 and row 3 were corrected on 2026-09-02
after all three ports were read side by side for the first time.  What that
turned up is the reason this section exists rather than being three closed items:
the "Verified Android behaviour (2026-08-07)" block cited nine symbols that exist
nowhere in cpmdroid, in its tree or anywhere in its history - scrollbackLinesEdit,
applyTerminalSettings, sendBytes, replyListener, userScrollUp++, snapToLive,
resetTerminal, historyIsContiguous and visibleHistorySize - and four of the claims
they carried asserted the opposite of what that code does.  The correction then had
to be corrected in turn: the first pass called cpmdroid's keeping of history across a
cold boot a defect, when clear() documents it as a considered product decision, and
it repeated the old block's misattribution of the capture choke point.  Reading a
port from outside is harder than it looks even when you know that is the failure.  Meanwhile ioscpm's
row 2 had been a bare unqualified tick since 2026-07-25 over a feature that had
never captured a single line, and its row 3 was a tick over a port with no text
selection at all.  Neither was caught because the cells were filled in from commit
messages: row 2 was flipped in d630619 twelve minutes after ioscpm's b703acf
landed, without that repository's source being opened.  The decision to make is
whether a tick is allowed to mean anything other than "somebody read this port's
code and can name the symbol".

- [WINDOWS] check-sibling-drift.sh now measures three things, and two of them
are new, so their false-positive rate is unproven.  The shipped-build field is
hand-maintained because no tree knows what a store is serving, and two of the
three ports are recorded shipped:unknown - which fails, deliberately.  Somebody
has to measure what Google Play serves for cpmdroid and what the romwbw_emu
release tag is; ioscpm's 37 came from the itunes lookup API and is real.  The
citation rule has now been run over the whole document: all thirteen rows carry
markers, 25 regions, and marking them is what corrected the extractor rather
than the other way round.  Every citation in the document now resolves in the
port it describes, at the commit that port's column was read at.

- [WINDOWS] the Store build is twenty-three source commits old.  Store 1.0.22
(confirmed 1.0.22.0 on 2026-09-03) and sideload 1.0.22-beta both came off the
2026-08-23 build, and z80cpmw/ has moved twenty-three commits since - the
attribute work and the palette this document already named, plus a download
thread outliving its dialog, the catalog shutdown ownership, a Keyboard page in
Settings, three Settings controls that read and wrote nothing, a downloader
hidden at 200% DPI, a Reset confirmation, bundled help topics and a truncated
download refusal.  Version.h still reads 1.0.22, so the numbers agree and prove
nothing; check-sibling-drift.sh measures from the v1.0.22-beta tag now and says
51 commits, of which those twenty-three are source.  Cutting 1.0.23 is the item.

- [DECISION] all three shipped: fields are now recorded - ioscpm 37 (measured
through the itunes lookup API), cpmdroid 25 (from the Play console, 2026-09-03),
romwbw_emu 1.38 (the v1.38 tag).  Two of the three still fail, and both failures
are real.  ioscpm's tree is at build 58 against a Store serving 37, twenty-one
builds of ticks describing software nobody can install.  cpmdroid and romwbw_emu
MATCH on the number and still fail, because a matching number is a weaker
statement than it looks: a version is bumped when somebody cuts a release, so
everything landing afterwards sits in the tree under the previous build's number.
cpmdroid is four commits past v1.24 and two of them are the scrollback fixes;
romwbw_emu is one past v1.38.  Both are measured from a tag and are exact.  The check says so now rather than reporting a
clean pass, which is what it did for romwbw_emu for exactly as long as it took to
record cpmdroid's number and notice the same thing was true next door.

- [BROWSER] the web half of the romwbw_emu column is read, not watched.  The
xterm CDN caveat that spanned it was closed by 5920681 on 2026-08-26 - vendored
under web/vendor with the tags pointing there - and this document went on
asserting it for a week, including through a column reading taken the day after
it closed.  That is worth keeping as the reason the citation rule exists.  What
nobody has done is open the page: that repo's own todo.txt carries a [BROWSER]
item saying no browser has drawn it since, web/ holds no .wasm at all since
2096ea2 and 95d422a, and make serve now fails on absent emcc rather than serving
a stale build.  Serving the unpacked deb is the way in.


Needs a person, not a machine
------------------------------

- see MANUAL_CHECKS.md.  Two checks are open there: the installed-MSIX file
transfer pass, which only a real MSIX install can reproduce, and the hands-on
pass over keystroke delivery, mouse copy/paste rendering and the first-run Help
auto-open.
