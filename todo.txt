Todo list.  Open items only, one line each.  The rules - what belongs here, and
that a closed item is deleted rather than annotated - are in CLAUDE.md.

  CHANGELOG.md       finished work
  MANUAL_CHECKS.md   checks that need a person at a keyboard
  KNOWN_PROBLEMS.md  standing facts that will never be done
  WIP.md             longer handoff notes

- [RELEASE] 1.0.34 is in review; measure the Store before believing it shipped.

- MANUAL_CHECKS 12: the listing screenshot is undersized and shows CBIOS 3.5.1.

- catalogv0::indexScope() still has no caller; the plan is in Config.h.

- the C4267 suppression on hbios_dispatch.cc is removable: romwbw_emu cast all
  six sites in 8eeb227, so the file is clean at /W3 from its v1.37 on.  Still
  present at z80cpmw.vcxproj:297.  Grep the MSBuild spelling
  `<DisableSpecificWarnings>4267`, not `/wd4267` - searching for the compiler
  flag reports it gone when it is not, which is how this nearly got deleted as
  stale.  Refiled from romwbw_emu 2026-09-15: the precondition landed there and
  shipped, and the line deletion is here.

- Delete catalogv0::runnableVersions and its caller in DiskCatalog once
  romwbw_emu drops the release gate; romwbw_emu/docs/RELEASE_GATE.md is why.

- README.md:76-79, CLAUDE.md:28-39 and FEATURE_PARITY.md:963-978 all promise a
  published release reaches users with no build of this app. It does not.

- core.romwbwVersion is never checked against what the core can boot, and
  Config.cpp:319-330 can write an unbootable one from a mounted image's name.
